var $=$||jQuery;$(document).ready(function(){function animateValue(id,start,end,duration){var obj=document.getElementById(id);var range=end-start;var minTimer=50;var stepTime=Math.abs(Math.floor(duration/range));stepTime=Math.max(stepTime,minTimer);var startTime=new Date().getTime();var endTime=startTime+duration;var timer;function run(){var now=new Date().getTime();var remaining=Math.max((endTime-now)/duration,0);var value=Math.round(end-(remaining*range));obj.innerHTML=value.toLocaleString('en-us');if(value==end){clearInterval(timer)}}
timer=setInterval(run,stepTime);run()}
const numAnim=document.querySelector('#animated-number');if(numAnim){animateValue('animated-number',parseInt(numAnim.getAttribute('data-from')),parseInt(numAnim.getAttribute('data-to')),1000)}
const trustCarouselEl=document.querySelector('.section--testimonials-secondary');if(trustCarouselEl){jQuery(trustCarouselEl.querySelector('.owl-carousel')).owlCarousel({items:1,loop:!0,nav:!0,autoplay:!0,autoplayTimeout:3000,autoplayHoverPause:!0})}})